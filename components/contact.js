var React = require('react');

var Contact = React.createClass({
  render: function(){
    return (
      <div>
        <h1>You weren't supposed to find this page.</h1>
        <h1>I don't actually want to talk to you.</h1>
      </div>
  )
  }
});

module.exports = Contact;
